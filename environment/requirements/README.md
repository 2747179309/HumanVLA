# 环境依赖记录

本目录用于保存经实际安装验证后的环境定义和锁定清单，例如：

- `motpose.environment.yml`：直接依赖与渠道。
- `motpose.explicit.txt`：同平台完整 Conda 构建清单。
- `motpose.pip-freeze.txt`：实际 pip 包版本。
- `openpose-build.txt`：OpenPose commit、CMake 参数和系统依赖。

当前尚未创建 `motpose` 或安装 OpenPose，因此不填写推测版本。
