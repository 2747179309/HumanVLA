# HumanVideo2VLA 工作规则

本文件适用于项目根目录及所有子目录。

1. 所有工作必须基于真实文件和真实命令输出。
2. 不允许编造实验结果、指标或参考文献。
3. 不允许把计划完成写成已经完成。
4. 不允许擅自改变论文主线。
5. 修改代码前必须先向用户说明计划。
6. 每次实验必须记录 Run ID、完整命令、参数、输入、输出、指标和错误。
7. 每轮工作结束必须更新 `tasks/CODEX_REPORT.md`。
8. 重要决定必须写入 `tasks/DECISIONS.md`。
9. 数据原文件只读，处理结果必须保存到新的目录。
10. 不得自动删除原始视频、标注、模型或实验结果。
11. 大规模下载、长时间训练、删除文件和修改系统配置前必须先征求用户同意。
12. 每个脚本必须支持 `--help`，包含基本异常处理和清晰、必要的注释。

## 数据与实验约束

- DeepSORT 的 `track_id` 只是单视频局部轨迹编号，不是真值，也不等于全局 `subject_id`。
- OpenPose 每帧 `people` 数组的顺序不稳定，禁止将数组下标当作人物 ID。
- MOT 结果必须经 CVAT 或等价工具人工复核，修正 ID Switch、断轨、误检和漏检。
- 原始文件放在 `data/raw_videos/`；任何抽帧、轨迹、骨架和标注衍生物写入对应的新目录。
- 不得用“效果好”“调试成功”等无证据描述替代命令、输入、输出和定量结果。

## 工作边界

- 当前优先实现 YOLO person 检测、DeepSORT、人工复核、BODY_25 提取和身份关联流水线。
- 当前不训练完整 OpenVLA，不下载大数据集，不运行未经批准的长时间训练。
- 论文结论必须受 `context/PAPER_CLAIMS.md` 约束。

## 论文算法阶段教学模式

从 T07C-B 开始，项目进入论文核心算法阶段。每个算法任务遵循 `context/RESEARCH_LEARNING_MODE.md` 定义的六层交付：
1. 理论讲解 2. 实验假设 3. 实现导读 4. 结果解释 5. 论文表达 6. 自测问题

Claude 同时担任研究导师和教学者，不仅仅是任务规划器。
